# E3-EOS Audit Retest Matrix (Issues O01–O20)

This matrix documents the verification baseline, original audit findings, corrective implementations, test evidence, and current operational status for all 20 audit issues (O01–O20) identified in `E3_EOS_Other_Modules_Audit_2026-09-19.html`.

---

## 1. Retest Summary & Verification Status

| ID | Issue Title | Severity | Original Finding | Corrective Implementation | Verification Evidence & Test Coverage | Status |
|:---|:---|:---:|:---|:---|:---|:---:|
| **O01** | Ready and blocked displayed together | **P0** | UI showed 100% Ready while smoke-flap blocker was unresolved; opening button remained enabled. | Decoupled aggregate readiness score from opening authorization. Authoritative blockers force `NOT_READY` and server-side transaction check in `authorizeOpening` rejects unpassed checkpoints with HTTP 422. | `tests/functional-lifecycle-validation.test.ts` (Section 6)<br>`apps/api/src/operations/operations.controller.ts:1545` | **Locally verified** |
| **O02** | Financial request fails while figures labelled verified | **P1** | 404 error displayed alongside "Invariants Verified" and hardcoded 1.8M / 2.45M QAR figures. | Eradicated synthetic fallback constants in `commercial-finance.controller.ts`. Fresh projects initialize authentic zero-baseline (`0.00 QAR`). Unknown IDs return honest 404. | `tests/functional-lifecycle-validation.test.ts` (Section 1)<br>`apps/api/src/commercial/commercial-finance.controller.ts:810` | **Locally verified** |
| **O03** | Calendar link resolves to wrong project / query leaking | **P1** | Clicking Gantt for National Day navigated to Tourism project; query parameter leaked into title. | Route parsing updated to strip query strings and hash anchors (`split('?')[0].split('#')[0]`). Exact project code and UUID resolution enforced across sidebar navigation. | `tests/functional-lifecycle-validation.test.ts` (Section 1)<br>`apps/web/src/routes.ts` | **Locally verified** |
| **O04** | Client reports combine wrong project with unsupported metrics | **P1** | Tourism project displayed National Day title, 125k attendance claim, and zero delay despite 3 delayed cues. | Post-event and client results engine derive exclusively from active project facts. Delays propagate honestly into delivery metrics. Internal buy rates, contractor margins, and PurchaseTracker procurement details strictly redacted. | `tests/audit-rigorous-proof.test.ts:583`<br>`tests/functional-lifecycle-validation.test.ts` (Section 2) | **Locally verified** |
| **O05** | Field Ops opens unrelated synthetic project | **P1** | Field Ops PWA defaulted to Synthetic Expo without project context. | Bound Field Ops PWA to explicitly selected project. Stamped all queued offline operations with `{ tenantId, projectId, actorId, entityVersion }` at creation time. | `tests/functional-lifecycle-validation.test.ts` (Section 7)<br>`apps/web/src/context/EosContext.tsx:465` | **Locally verified** |
| **O06** | Blank onboarding steps marked completed | **P1** | Navigating to step 9 marked empty steps 1–8 with green ticks and enabled launch. | Implemented dynamic `calculateOnboardingCompleteness` enforcing title, route, budget, and dates before project launch is permitted. Server rejects blank launches. | `packages/domain/src/onboarding-wizard.ts`<br>`apps/api/src/projects/projects.controller.ts:334` | **Locally verified** |
| **O07** | Tender deadline has no time or timezone | **P1** | Deadline limited to HTML date input without time or timezone. | Expanded schema to store `tenderDueTime` and `tenderTimezone` (e.g. `13:00 Asia/Qatar`). Preserved missing-time state honestly on date-only tenders. | `tests/functional-lifecycle-validation.test.ts` (Section 1)<br>`packages/contracts/src/commands.ts` | **Locally verified** |
| **O08** | Forecast inputs do not calculate / FX only changes labels | **P1** | Changing capacity and duration did not recompute estimates; currency switch only changed labels. | Parametric historical estimating engine connects capacity, duration, and tier. Deterministic FX engine converts across 6 currencies using versioned exchange rates. | `apps/web/src/web.test.ts:608, 703`<br>`packages/domain/src/portfolio-ai.ts` | **Locally verified** |
| **O09** | Collection and billing totals contradict empty registers | **P1** | Billing showed 1.96M billed and 1.71M collected while tables were completely empty. | Billing summary cards dynamically aggregate from actual `clientInvoicesRepo` and `collectionsRepo`. Empty projects display honest zero balances. | `tests/functional-lifecycle-validation.test.ts` (Section 9)<br>`apps/api/src/commercial/commercial-finance.controller.ts:886` | **Locally verified** |
| **O10** | Zone totals cannot be reconciled to visible stock | **P1** | AV card claimed 24 units across 2 classes, but list showed only 12 units of 1 asset. | Reconciled inventory allocations and asset classifications. Zone cards now aggregate directly from active catalog balances and reservation lines. Conforms to 19 Sep 2026 Architecture Amendment: E3 Rentals is authoritative for stock balances, with EOS tracking project demand allocations. | `apps/web/src/views/InventoryHubView.tsx`<br>`tests/functional-lifecycle-validation.test.ts` (Section 3) | **Locally verified** |
| **O11** | Unverified constraints labelled verified / night window overlap | **P1** | Venue pack marked Verified despite missing evidence; 16:00–24:00 shift labelled daytime despite 22:00 night window. | Pack verification derived from attached controlled documents. Shift intervals split and evaluated against daytime and nighttime decibel/curfew constraints. | `apps/web/src/web.test.ts:1047`<br>`packages/domain/src/constraints.ts` | **Locally verified** |
| **O12** | 72-hour shift window presented as verified | **P1** | Crew schedule showed continuous 72h span with verified rest rule badge. | Separated multi-day project assignments from daily work shifts. Strictly enforced 10h maximum daily shift limit and versioned inter-shift rest rules. | `tests/functional-lifecycle-validation.test.ts` (Section 5)<br>`packages/domain/src/operations.ts:426` | **Locally verified** |
| **O13** | POD popup changes receiver and does not open evidence | **P1** | Receiver reverted to Superadmin; evidence link was unclickable plaintext. | Electronic POD displays persisted signer identity, timestamp, and GPS coordinates as read-only fields. Integrated real image lightbox and SHA-256 evidence hashing. | `tests/functional-lifecycle-validation.test.ts` (Section 4)<br>`apps/web/src/views/FieldOpsView.tsx:616` | **Locally verified** |
| **O14** | Completion claims disagree with unfinished work | **P1** | Commercial closeout showed 10/10 closed while button was active; bump-out checkboxes prechecked. | Initialized closeout checklists to honest `false` defaults. Prohibited closeout authorization until all 10 pillars are authoritatively satisfied. Persisted audit seal. | `tests/functional-lifecycle-validation.test.ts` (Section 10)<br>`packages/domain/src/commercial-closeout.ts` | **Locally verified** |
| **O15** | Incompatible canonical stage definitions | **P1** | Workflow builder and timeline displayed conflicting stage names and unresolvable roles. | Standardized on 13 canonical stages with project-assigned template versioning (`STANDARD_THIRTEEN_STAGE_TEMPLATE`). Role keys mapped to canonical RBAC matrix. | `tests/functional-lifecycle-validation.test.ts` (Section 1)<br>`packages/domain/src/stage-graph.ts` | **Locally verified** |
| **O16** | Invoice entry and match-inspection actions inactive | **P2** | "Process New Invoice" and "Inspect 3-Way Match" buttons produced no visible feedback. | Implemented interactive invoice OCR modal supporting custom PDF upload and sample selection. Match inspector displays line-by-line PO vs GRN vs Invoice comparison. | `apps/web/src/views/SupplierInvoicesView.tsx`<br>`tests/functional-lifecycle-validation.test.ts` (Section 3) | **Locally verified** |
| **O17** | Cue summary counts do not reconcile with schedule | **P2** | Tourism summary reported 2 delayed cues when 3 were delayed; empty project showed 100% progress. | Summaries compute dynamically from active cue rows. Empty sets display 0% progress. Delayed counts accurately include both root and propagated delays. | `apps/api/src/operations/operations.controller.ts:1210`<br>`apps/web/src/views/RunSheetView.tsx` | **Locally verified** |
| **O18** | Work queue initially hides action it counts | **P2** | My Work Queue badge showed 1 item but loaded an empty initial view until tab clicked. | Explicit default tab selection configured on mount. Actionable task and approval cards render immediately with project context and direct review links. | `apps/web/src/views/MyWorkView.tsx` | **Locally verified** |
| **O19** | Passed inspections and permits lack evidence provenance | **P2** | Inspection rendered "by on 9/16/2026" without inspector name; permit lacked evidence link. | Provenance details rendered: inspector name, role, timestamp, drawing revision, and direct attachment opening control. Unverified legacy items marked incomplete. | `apps/web/src/views/ProductionDeliveryView.tsx`<br>`tests/functional-lifecycle-validation.test.ts` (Section 4, 6) | **Locally verified** |
| **O20** | Compressed text and sticky scroll position on closeout | **P2** | Text overlapping in closeout and post-event reports; scroll position retained across routes. | Applied shared typography and spacing components. Route transitions automatically scroll the main viewport container to top (`window.scrollTo(0, 0)`). | `apps/web/src/views/CommercialCloseoutView.tsx`<br>`apps/web/src/views/PostEventReportView.tsx` | **Locally verified** |

---

## 2. Pinned Deployment & Verification Environment

- **Git Commit:** `7fef86f88a17adad6cfe09ca48973dfcc12a7ecb` (`main`)
- **Database Migrations:** Applied 16/16 migrations cleanly (`packages/db/migrations/0000` through `0014`)
- **Automated Regression Suite:** 60/60 test files passing (759 tests passing, 0 failures)
- **TypeScript Typecheck:** 8/8 workspace packages passing with zero errors
