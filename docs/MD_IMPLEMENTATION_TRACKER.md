# Markdown implementation tracker

Date: 26 September 2026. Baseline commit: `3cb63f77ab4b044e63c72831ad180c840889c229` plus the current uncommitted workspace. This is a local implementation record, not production acceptance.

## Governing documents

1. [Current decisions](../E3_EOS_Complete_MD_2026-09-26/00_CURRENT_DECISIONS_AND_READING_ORDER.md) and package README.
2. Master handover architecture/product contract and focused specs/phases/stages.
3. Updates 01–05 supersede older provisions for their subjects.
4. The user's subsequent glass/3D UI, original E3 branding, light/black-dark theme instructions supersede the older visual prohibition on glass. Other accessibility, role, data and audit requirements remain.

The existing Vite/React, NestJS, PostgreSQL monorepo is retained. The old proposed Next/Nest stack does not authorize an unreviewed platform migration. Local tools observed: Node 24.19.0, pnpm 11.19.0. Actual dependency versions remain in package manifests/lockfile.

## First implementation increment

| Requirement | Change | Evidence and limit |
|---|---|---|
| P00-ST04; AT-001/003/006 | Protected session guard requires a database session and active membership; database failure denies access. Caller identity/role/super-admin headers cannot establish authority. Synthetic identity/JIT/UAT mode requires explicit local opt-in. | `tests/foundation-session-boundary.test.ts`; does not establish that every controller has the correct guards/audience/project-grant checks. |
| P00 AT-010 | Explicit credentialed-origin configuration and unsafe cookie-command origin checks. | Foundation security tests; deployment origins still require environment configuration. |
| P01-ST06; exact-version decisions | Reject unknown/cross-scope requests, stale reviewed versions/hashes, repeated decisions, missing trusted deciders, wrong roles and unverifiable/self approvals. Stop client placeholder hashes and conditional-to-approved conversion. | `tests/approval-decision-boundaries.test.ts`. Legacy request creation, transaction-bound target snapshots, full quorum/delegation and durable approval history remain incomplete. |
| P01 screens; update 04/05 | Approval queue failures are visible; decisions submit the displayed version/hash and preserve feedback on failure. Remove synthetic personal approval records and false signed state on request failure. | Web typecheck and regression tests, plus browser review recorded separately. |
| Updates 01/05; addendum chain | Scope comparisons/deltas to project/org/source jobs; reject ambiguous, cross-project, mismatched, stale or already-reviewed targets before mutation. Preserve explicit zero/unknown values and prior wording/quantity. No fabricated operational counter/zone/variant defaults. | `tests/addendum-revision-safety.test.ts` and parser regression suites. Requires subsequent durable storage and full record-version concurrency/provenance work. |
| P04 offline; update 05 #7 | Captures remain provisional and retained on the device. Fake sync success and clearing of unacknowledged captures are removed. Recover legacy simulated-synced records to an explicit error state. | `apps/web/src/services/offline-sync.test.ts` and web tests. At this increment, legacy sync acknowledged only in memory. The sixth increment adds a separate text-observation receipt flow; legacy media/sync remains unavailable. |

## Second implementation increment — membership administration and startup

| Requirement | Implemented behavior | Evidence and limit |
|---|---|---|
| P00-ST03/04; no demo startup side effects | DB imports are read-only in effect; seeding is an explicit CLI operation with transaction/rollback and nonzero failure exit. | Fresh production-mode import probes plus real before/after API restart fingerprints. Explicit seeding still resets synthetic fixture data when invoked. |
| P00-ST04/05; AT-001/003/009 | Membership revocation rechecks live session/internal Super Admin authority, rejects self/last-admin and cross-org targets, and commits membership + audit + outbox + durable receipt atomically. Replays preserve original response. | 20 PostgreSQL tests cover concurrent opposing revocations, duplicate retries, stale actor authority and injected write failures. Process-restart HTTP replay is recorded separately. No migration added. |
| P00 admin read scope | Directory and capabilities resolve current DB authority; only current-org memberships are returned, including actual revoked status. `/me` uses the actual role and `/audit-events` reads scoped durable metadata. | 13 admin boundary tests and HTTP checks. This is not proof that all other controllers enforce project grants. |
| P00 truthful interactions | Removed fabricated users, local impersonation fallback and fake invitations/access grants. Admin UI supports search, status filters, error/retry, reasoned revocation and local-only role preview capability. | 14 client tests and desktop/mobile English/Arabic light/dark browser checks. New invitation, role-change, restore and project-grant commands return explicit 503 while incomplete; no email is sent. |
| Update 05 environment/evidence | Header displays API-reported environment; health distinguishes process liveness from unverified governance controls, and missing build SHA remains unknown. | 18 metadata tests plus existing API health regression. No guessed cloud region or staging designation. |
| API contract | Runtime OpenAPI 1.1.0 documents membership revocation, scoped directory/audit reads and unavailable legacy operations. | Both runtime contract copies match; 5 contract checks. Supplied handover documents remain unchanged. |

## Third implementation increment — controlled invitations

| Requirement | Implemented behavior | Evidence and limit |
|---|---|---|
| P00-ST04; invitation-only accounts | Scoped current internal Super Admin can queue and cancel invitations with a reason. No user or membership exists until valid acceptance. Privileged roles, existing platform administrators, unknown roles and legacy invitations are blocked. | Migration 0015 plus real PostgreSQL lifecycle tests. Privileged recent-MFA governance remains unfinished. |
| P00-ST05; durable commands | Creation, cancellation and acceptance lock current authority and commit audit, outbox and idempotency receipts atomically. Expiry, cancellation, stale inviter and replay conflicts fail closed. | Race, rollback-injection and independent-controller tests; live HTTP and process-restart evidence recorded below. |
| Existing account protection | Acceptance requires a matching live session for an existing account; credentials/name are never overwritten. Existing or revoked memberships cannot be changed/restored through invitations. | Tests cover unauthenticated, wrong-identity and simultaneous acceptance, exact credential preservation and post-revocation historical replay. |
| Delivery truthfulness | Random single-use token hash is stored; token/link are AES-256-GCM encrypted in a queued outbox intent. No plaintext token/link is returned in API receipts, directory reads or audit events. | Delivery key/origin/TTL configuration gates creation. No sender is activated; queued does not mean emailed or received. |
| English/Arabic interface | Administration provides invitation role/reason form, pending/history views, cancellation and error/retry. Acceptance uses E3 glass branding, light/black-dark themes, current invitation metadata and distinct new/existing account paths. | Desktop and 390 × 844 mobile RTL visual checks. Tokens are removed from URLs and kept only in memory; reload requires reopening the link. |
| Verified browser identity | Removed browser-created login fallback and canonical-role substitution; reload and sign-in use server session identity/organization. | Client tests prove rejection/incomplete responses cannot become successful sessions. Existing fixture credentials still work against the real local DB. |
| Contract/setup | Both OpenAPI mirrors are version 1.2.0; local startup helper loads a Windows-protected queue key and leaves synthetic auth off. | Seven OpenAPI checks; local migration preserves identity counts. No deployment or production acceptance claimed. |

## Fifth implementation increment — project work, documents and reporting batch

Status: **implemented and locally verified**, with the limits in the [fifth-batch evidence](../artifacts/module-batch-verification-2026-09-26.md). The related modules were built together before one combined verification phase and focused corrections. This is a local candidate, not phase acceptance or a release.

| Requirement | Implemented behavior | Current boundary |
|---|---|---|
| P00 project/action boundary; P01 ordinary work | Shared project transaction rechecks current session, active membership, internal audience, explicit project grant and action permission. Durable command runner commits domain writes, audit, outbox and stable retry receipt together. Nested assignment/classification hooks run before replay where required. | This extends named modules below, not every controller. Grants never follow ownership or global administrator status automatically. Outbox intent is not delivered notification evidence. |
| P01 work packages and tasks | `GET/POST /projects/{projectId}/work-packages` and `GET/POST /projects/{projectId}/tasks` use PostgreSQL. Owners/assignees need current internal membership and project grants; optional stages and packages must belong to the same project. Task completion requires current action authority, editor grant, reason and expected version. Field supervisors read/complete only assigned tasks. | Completion leaves package acceptance unchanged. Text completion evidence is an operator note, not proof of controlled-file verification. Gantt authoring, dependencies, package acceptance and protective-action commands remain explicitly unavailable over HTTP. |
| P02 document identity and revision metadata | `GET/POST /projects/{projectId}/documents`, scoped detail and revision routes use durable records. Reads filter classification. Draft register/revision commands require editor grant plus `projects.manage` or `design.version`; commercial sensitivity requires additional current authority. Revision commands increment parent rowVersion without advancing an approved-current pointer. | Missing file remains missing. No generated file bytes, passed scans, hashes, approvals, signed URLs or downloads are fabricated. Legacy rows retain their stored facts but are projected as unverified. Personnel-sensitive/restricted-incident records remain unavailable pending narrower policy. Lists expose a 500-record bound and truncation flag. |
| P02 controlled-file boundary | All document HTTP handlers receive the current project boundary. Legacy transmittals, required slots, working copies, comments and submission packs fail closed until durable implementations exist. Company vault legacy HTTP is unavailable; direct compatibility scenarios require explicit test-only synthetic opt-in without an HTTP request/session. | Private object storage, actual retained immutable bytes, quarantine scanning, approval/release, signatures, pack assembly and explicit client publication remain open. Blocking fabricated success is not completion of these workflows. |
| P05 internal report snapshots | `GET/POST /projects/{projectId}/reports`, scoped detail and new-version commands capture canonical project/stage/activity states, source manifest, as-of time and content hash. Report revisions are separate immutable rows; prior versions are not overwritten. | Reports are internal drafts. Requested dates label the report; captured state is not period-filtered performance. Financial, attendance, marketing and satisfaction values are unavailable. Publication, closure-by-dimension and lessons commands remain unavailable. |
| P05 scoped portfolio reporting | `GET /portfolio/summary` counts only projects with current explicit grants and reports actual maturity/outcome distributions plus internal report-series/version counts. | No inferred revenue, cost, margin, staffing capacity, acceptance, readiness or settlement. Empty scope gives real zero counts. Broader legacy portfolio controllers are outside this implementation claim. |
| API contract and schema | Runtime OpenAPI mirrors were verified at version **1.4.0**, with request metadata, response states, durable key semantics, optimistic-version errors and unavailable operations. Migrations `0018_work_versions.sql`, `0019_document_versions.sql` and `0020_reporting_snapshots.sql` were applied transactionally to verified loopback PostgreSQL. | Ledger reached 22 entries with no grant backfill or identity/project count changes. [Migration evidence](../artifacts/module-batch-migrations-2026-09-26.json). Supplied MD package remains unchanged. |

The combined build passed. The initial full regression reported 995 passing tests, six failures and one web transform failure; all discovered failures were resolved in focused runs (93 web/work, 17 document/contracts and 25 persistence/constraints, with client/route/membership cases also covered). No final full-suite rerun is claimed. All 80 live HTTP checks passed, including process-restart receipt/history persistence. Browser checks covered desktop dark and mobile Arabic/light access states; the signed-in account had no project grants, so populated commands were exercised with isolated HTTP fixtures. Local P00/P01/P02/P05 scope and production acceptance remain open.

## Sixth implementation increment — requirement intake, clarifications and field observations

Status: **implemented and consolidated verification completed**. This bounded batch follows [update 01](../E3_EOS_Complete_MD_2026-09-26/updates/01_REQUIREMENTS_SCOPE_AND_DESIGN.md), P01-ST04 and the observation-only portion of P04-ST06. Migrations 0021–0023 were applied transactionally, all workspace TypeScript checks passed, and the full regression passed **87 files / 1,151 tests**. Workspace build and 86 live HTTP checks passed, including process restart. Final web TypeScript/build and 25 focused OpenAPI checks passed; browser coverage and its no-project-grant limit are recorded in the evidence. [Sixth-batch evidence](../artifacts/control-batch-verification-2026-09-26.md) records the consolidated results and limits; no phase acceptance or release is claimed.

| Requirement | Implemented behavior | Current boundary |
|---|---|---|
| P01-ST04; one requirement authority | `GET/POST /projects/{projectId}/scope-register/requirements`, scoped detail and `/{id}/revisions` operate on the existing `requirements` and `requirement_revisions` tables. Manual draft intake preserves original wording separately from interpretation. Unknown quantity/unit/dates stay null; explicit decimal-string zero is retained. New revisions retain a stable requirement ID, immutable snapshots, content hashes and a current-revision pointer. | New records are unapproved drafts with unverified source claims and no published operational scope. Legacy rows remain `legacy_unverified`; approved, legacy or history-inconsistent records cannot be revised through this draft flow. No parser publication, allocation, disposition, design release or downstream acceptance is implied. A stored hash proves snapshot consistency, not source authenticity. |
| P01 clarification register | `GET/POST /projects/{projectId}/scope-register/clarifications`, scoped detail, `/{id}/respond` and `/{id}/reopen` record internal questions, attributed responses and immutable action history. Owners/respondents need current internal membership and a same-project grant. Optional requirement references pin the specific controlled requirement version and immutable revision. | Respond/reopen require a reason and expected version. Reopening retains earlier responses. Attribution is unverified text; no client delivery, source-authority verification or scope approval occurs. Existing legacy questions are not silently promoted into this register. |
| P00 current authority and durable commands | Both intake registers use current session/membership, internal audience, explicit viewer/editor project grants and an existing action permission (`projects.manage`, `design.version` or `operations.manage`). Writes record domain state, audit, outbox and durable retry receipts in one project transaction. Changed authority is checked before replay. | Same-organization and nested-project checks also apply to owners and linked records. Stale updates return conflicts instead of overwriting reviewed state. This covers the named registers, not all legacy scope/design controllers. |
| P04-ST06 text observation receipts | `GET/POST /projects/{projectId}/field-observations` and scoped detail append low-sensitivity notes with actor/project, reason, client operation/device IDs, claimed capture time, server receipt time and optional task/base version. Current permission is `projects.manage`, `operations.manage` or `field.inspect`, with viewer reads/editor writes. Field supervisors see their own observations and may link only currently assigned tasks. | Domain row, audit, outbox and transport receipt commit atomically. The actor-scoped client operation ID deduplicates retries even under a new transport key. A stale task version is retained as `accepted_as_observation_with_conflict`; it never completes the task or changes safety, approval, qualification or release state. Device identity and capture time remain claims. |
| English/Arabic project registers and device queue | Requirements and clarifications have project selectors, server-capability controls, full reviewed revision/reason forms, history and source-state disclosures in the existing glass light/dark UI. Field notes use organization/actor-scoped local storage, explicit sync, receipt matching and explicit clearing of acknowledged copies only. | Local saving is provisional and does not confer server authority. Storage can be lost; this is not the full offline work bundle or media queue. Unsupported old captures are retained. No files or sensitive incident payloads are accepted by the text endpoint. UI and device behavior still require the consolidated checks. |
| Legacy HTTP and schema | Legacy `ScopeController` HTTP is guarded by current internal project access and then returns `503 LEGACY_SCOPE_WORKFLOW_UNAVAILABLE`; legacy `FieldSyncController` HTTP returns `503 LEGACY_FIELD_WORKFLOW_UNAVAILABLE` after the tenant boundary. Migrations `0021_requirement_intake_versions.sql`, `0022_clarification_records.sql` and `0023_field_observations.sql` add versions, scoped links, immutable-history update guards and observation receipts. | All three migrations were applied in one transaction; ledger reached 25. Before/after counts remained 75 users, 75 memberships, 13 projects and zero project grants. Legacy direct fixtures do not establish usable HTTP workflows. All 86 live HTTP checks passed, including legacy-route unavailability; no grant backfill, production deployment, connector activation or phase closure is claimed. |

The routes are `/requirements/register`, `/clarifications/register` and `/field/notes`, with project routes `/projects/:id/requirements`, `/projects/:id/clarifications` and `/projects/:id/field-notes`. The passing combined regression includes register scope/version/history/retry and field receipt/queue tests. Build and real HTTP persistence passed. Desktop EN/dark and mobile AR/light access-state layouts were checked; populated browser forms and physical-device offline acceptance remain unverified.

## Seventh implementation increment — allocation planning, design briefs and impact review

Status: **implemented and locally verified within the boundaries below**. The three connected modules were developed together, following [update 01](../E3_EOS_Complete_MD_2026-09-26/updates/01_REQUIREMENTS_SCOPE_AND_DESIGN.md) and [the data-model invariants](../E3_EOS_Complete_MD_2026-09-26/specs/03_DATA_MODEL_AND_INVARIANTS.md), then checked in one consolidated phase with focused corrections. [Seventh-batch evidence](../artifacts/planning-batch-verification-2026-09-26.md) records migration, regression, build, HTTP/restart and browser results. This is a bounded local candidate, not P00 closure, human UAT sign-off or release acceptance.

| Requirement | Implemented behavior | Current boundary |
|---|---|---|
| Update 01; canonical allocation identity | `GET/POST /projects/{projectId}/allocation-register`, scoped detail and revision routes extend the existing `requirement_allocations` authority. Draft allocations pin an explicit current requirement version, immutable revision and hash. Location, zone and department remain separate dimensions; grouping changes only the view. Unknown quantity/unit/location/owner stays null and explicit zero is retained. | Only recorded, unapproved, unarchived requirement drafts can support new planning revisions. The allocation keeps its requirement identity. Legacy allocations stay unverified and read-only. Later source changes flag stale pins without rewriting allocation history; a deliberate revision can select the current source. No approved scope, delivery state, acceptance or production release is created. |
| Update 01; quantity basis | PostgreSQL numeric arithmetic totals known quantities with matching unit labels across the controlled register, separately exposing unknown quantities, unknown/different units and earlier source pins. Arithmetic difference is unavailable when values or units cannot be compared. | Quantity-basis applicability remains unknown. No quantity partition rule, approved rounding tolerance, unit conversion, commercial allocation rule or compliance/accepted-quantity decision is declared. A difference is arithmetic information only. Lists are bounded; UI regrouping does not alter records or totals. |
| Update 01; versioned design handoff | `GET/POST /projects/{projectId}/design-register`, scoped detail and revision routes use canonical `design_packages` plus immutable `design_package_revisions`. Metadata briefs retain explicit requirement and optional allocation revision/hash references. New revisions require a reason, current target version and current verified source pins; earlier brief snapshots remain available. | This is a metadata-only internal draft brief. File status is missing, approval state is not approved and production release is false. No file upload, retained bytes, scan result, drawing approval, client submission or fabrication instruction is manufactured. Legacy design records remain unverified and unavailable for draft revision. |
| Update 01; downstream impact review | `GET /projects/{projectId}/impact-register` compares pinned source revisions with current controlled requirement/allocation revisions. `GET/POST /projects/{projectId}/impact-register/assessments` records immutable advisory assessments against the exact target version and server-derived impact fingerprint, with reason and proposed follow-up. | An assessment does not resolve staleness, automatically rebase an allocation/design, revise requirements, approve scope or release work. Actual draft correction remains a separate reviewed revision. The register inspects a bounded window of 200 recent eligible targets; its counts describe that inspected window, not every project obligation. Design impacts also require design action authority. |
| P00 current authority and durable commands | All three registers use current database sessions, internal membership, explicit project viewer/editor grants and action permissions. Allocation/impact actions accept one of `projects.manage`, `design.version` or `operations.manage`; design actions require `projects.manage` or `design.version`. Writes atomically retain domain state, audit, outbox and stable-key receipt. Nested source/owner scope checks precede replay. | Ownership or Super Admin status does not replace a project grant. Expected versions and content fingerprints reject stale requests. An outbox intent is not delivered notification evidence. This candidate extends the named modules only and does not certify every controller or close P00. |
| English/Arabic project planning interface | Allocation planning, design briefs and change impact are connected through project-scoped glass light/dark views. Forms preserve full draft values, reasons, stable retry keys, source review and history. Allocation grouping separates physical location from department responsibility; changed source selection requires deliberate review. | Desktop English/dark and mobile Arabic/light navigation/access states were checked for all three modules. At 390 × 844, measured content width and scroll width were both 380 pixels, with no horizontal overflow. The account has no project grants, so populated browser forms remain unverified; commands were exercised through service and isolated HTTP fixtures. |
| Legacy HTTP and schema | `LegacyDesignBoundaryGuard` checks current internal project access, then legacy design HTTP returns `503 LEGACY_DESIGN_WORKFLOW_UNAVAILABLE`. Migrations `0024_allocation_register.sql`, `0025_design_register.sql` and `0026_impact_review_records.sql` were applied transactionally to verified loopback PostgreSQL, adding scoped links and immutable revision/assessment update guards. | Ledger reached 28 entries. Counts remained 75 users, 75 memberships, 13 projects and zero grants. The allocation-to-requirement scope FK uses `NOT VALID` to preserve historical rows while enforcing new writes; historical rows are not claimed fully validated. Legacy parser, scope approval, file upload, design approval and fabrication release remain unavailable. No grant backfill, connector activation or production deployment occurred. |

The new entry routes are `/allocations/register`, `/designs/register` and `/impact-review`, with project routes `/projects/:id/allocations`, `/projects/:id/design-briefs` and `/projects/:id/impact-review`. Verification covers authority/scope, source integrity, immutable history, null/zero quantities, exact arithmetic, optimistic concurrency, receipt retries and transactional rollback.

The full regression ran 93 files / 1,207 tests: 1,205 passed and two allocation revocation fixtures failed because their test setup omitted required revocation actor/time. After fixing those fixtures, the focused five-file planning set passed **47/47 tests**. No final full-suite rerun is claimed. Final workspace TypeScript checks passed. The initial workspace build passed every package except an API declaration-export issue; the API build passed after its focused correction, so all workspace outputs were built. The web bundle warning remains open at 2,237.87 kB minified / 517.56 kB gzip. Both OpenAPI mirrors are version **1.6.0**.

All **63/63 live HTTP checks** passed, including actual API process restart **9952 → 23372**, with 16 audit events, 16 outbox records and 16 durable receipts retained for the fixture commands. Exact cleanup removed the two isolated organizations and five owned users. No existing-user grants were changed. Browser access-state checks do not substitute for populated-form or human UAT acceptance; the quantity-basis, metadata-only design and advisory-assessment limits above remain in effect.

## Eighth implementation increment — forecasts, dependencies and calendar

Status: **implemented and locally verified within the stated scope**. This connected batch implements manual forecasts and dependency planning from P01-ST05, following the workflow/schedule separation in specification 04. [Eighth-batch evidence](../artifacts/schedule-batch-verification-2026-09-26.md) records the checks and limits.

| Requirement | Implemented behavior | Current boundary |
|---|---|---|
| P01-ST05; forecast versus baseline/actual | Canonical tasks retain separate forecast start/finish, source IANA timezone, forecast version and immutable revisions. Commands require reviewed task and forecast versions plus a reason. Exact offset instants normalize to UTC; clearing all forecast fields appends history. | Open tasks in active packages only. Forecast changes increment the task version. Actual completion is unchanged. No contractual baseline amendment, resource reservation, cost approval or work acceptance is implied. |
| P01-ST05; dependency DAG | Canonical dependency edges support scoped FS/SS/FF task links, stable retries, current task versions, retained create/archive history and duplicate/self/cycle rejection. | Maximum 1,000 active connections; legacy or inconsistent edges block expansion. Timing consistency checks do not move tasks or authorize execution/acceptance. |
| Calendar and project timeline | `/schedule`, project schedule routes and the cockpit display persisted forecasts/dependencies. `/calendar` replaces fixed sample events with a scoped UTC month and agenda, project filters and explicit errors. | Timeline limits: 500 tasks, 1,000 active and 200 archived dependencies. Calendar windows are at most 366 days and 1,000 overlapping forecasts. Field supervisors see assigned tasks only. Unscheduled tasks remain in the project schedule. |
| Current authority and durability | Internal membership and explicit project grants protect reads; permitted work roles plus editor access protect commands. State, immutable history, audit, outbox and receipts commit atomically. Current scope is checked before replay. | No existing grant changes, administrator bypass, external connector activation or production acceptance. |

Migrations `0027_task_forecasts.sql` and `0028_task_dependencies.sql` were applied transactionally, bringing the ledger to 30 entries. Counts stayed at 75 users, 75 memberships, 13 projects and zero project grants.

The combined regression ran **96 files / 1,234 tests**: 1,229 passed and five new dependency tests failed on one PostgreSQL UUID/text parameter mismatch. That mismatch was corrected, then all **27 scheduling service/client/OpenAPI tests passed**. No second full-suite pass is claimed. Workspace TypeScript checks and build passed; final web TypeScript/build checks also passed. OpenAPI mirrors are version **1.7.0**.

All **58/58 live HTTP checks passed**, including real API restart **34460 → 29040**, retained forecast/dependency receipts and histories, stale/cyclic command rejection, assignment scope, clearing forecasts and denied replay after revocation. The fixture commands produced 21 audit events, 21 outbox records and 21 durable receipts. Cleanup removed two isolated organizations and six owned users.

Browser checks covered desktop English/dark scheduling access states and calendar navigation, plus Arabic/light at 390 × 844 with no horizontal overflow. The existing browser account has no project grants, so populated forms and month cells remain visually unverified; populated commands were exercised through service and HTTP fixtures. Approved baselines, working calendars, recurring periods, milestones, lag, capacity leveling and governed rescheduling remain open.

Next scheduling increment: governed baselines and working calendars, followed by separately reviewed rescheduling scenarios. Baseline amendment authority, timezone transitions, immutable history and large-graph limits need explicit implementation. Forecast consistency must remain separate from capacity and execution approval.

## Ninth implementation increment — working calendars, milestones and baseline candidates

Implemented as one connected batch following P01-ST05 and specification 04. The schedule area now contains working calendars, milestone planning and baseline candidate review alongside task forecasts/dependencies.

- **Working calendars:** named local-time definitions with all seven weekdays, up to four non-overlapping intervals per day and up to 100 date overrides. Overrides replace that local day's usual hours, including explicit non-working days. Overnight intervals are split across days. Each revision requires the reviewed version and reason; earlier definitions remain available. Nominal weekly hours are not elapsed DST-adjusted hours or capacity availability.
- **Planning milestones:** named targets with exact offset instants and IANA timezone, or both unknown. Notes, reason and immutable version history are retained. Targets do not declare contractual commitments, actual completion or acceptance.
- **Baseline candidates:** review a server-generated source containing complete scoped task forecasts/actual completion, active controlled dependencies and current calendar/milestone definitions. Capture checks the reviewed source fingerprint and freezes an immutable candidate. Read-only comparison shows changed task versions/dates, finish shifts in elapsed minutes and changed planning definitions/dependencies. Missing forecasts remain unknown. A candidate is explicitly unapproved; it neither publishes nor amends an approved baseline.

Current sessions, internal membership, explicit project grants and action permissions protect the register. Field supervisors cannot read project-wide planning snapshots. Commands use stable keys and commit state, history, audit, outbox and receipts together; current authority is checked before replay. No existing-user grants are added. Definition limits are 100 calendars and 200 milestones per project; lists show the latest 25 candidates and histories show the latest 100 revisions. Candidate capture fails instead of truncating beyond 500 tasks, 1,000 active dependencies or 300 definitions, and rejects legacy/inconsistent sources.

Migration `0029_schedule_planning_register.sql` was applied transactionally; ledger 31, unchanged 75 users, 75 memberships, 13 projects and zero grants. OpenAPI mirrors are version 1.8.0. One focused end-of-batch check passed **13 tests across three files**; API/referenced-package build, web TypeScript and web build passed. API restarted successfully as PID 27172. No full audit, repeated regression cycle or populated browser acceptance is claimed. See [batch completion notes](../artifacts/schedule-planning-batch-2026-09-26.md).

Approved baseline publication/amendment authority, applied working-time calculations, recurring periods, dependency lag, capacity leveling and rescheduling scenarios remain open.

## Ordered remaining backlog

These are confirmed gaps, not assumed completed because a screen or old report exists.

| Priority / phase | Next work | Exit evidence |
|---|---|---|
| P0 / P00 | Inventory every controller's verified identity, audience, project grants, document/export/search scopes; remove residual default authorities and unguarded command paths. | Real HTTP forged-header, revoked-member, wrong-audience and cross-project negative tests. |
| P0 / P00 | Extend the implemented grant/role/restoration boundary to remaining modules. Complete invitation delivery/acknowledgement, privileged recent-MFA governance, controlled recovery and synthetic-job removal; isolate legacy test fixtures in their own database. | No membership granted before acceptance, no ownership substitution, concurrent authority checks, durable receipts/audit and per-project HTTP denial tests. |
| P0 / P00–P01 | Replace Map-backed approval/policy/exception commands with transactional PostgreSQL repositories, durable idempotency/audit/outbox and current policy authority. Request creation must bind real target revisions/content instead of placeholder IDs or random hashes. | Concurrent independent processes, crash/restart replay, failed-write rollback and authority revocation tests. |
| P0 / P02 | Complete actual immutable file retention, quarantine scanning, exact revision assembly and designated release/signature authority. The fifth batch verified the metadata-only boundary and blocked legacy upload/issuance claims; it did not implement these file workflows. | Independent PDF render/content checks, checksum/manifest, permission, scan and revision tests. |
| P1 / P01 | Extend the locally verified allocation/design/impact planning registers with approved-baseline change authority, source-evidenced parser publication, declared quantity bases and controlled downstream release. | Spoofed-override rejection, approved-history preservation, real source evidence, concurrency and restart tests. Draft intake, planning quantities, metadata briefs and advisory assessments are not scope approval or release. |
| P1 / P04 | Extend the verified text-observation transaction and receipt-based device queue to assigned-work bundles, retained media, pending-evidence review and current-authority reconciliation. | Offline/retry/reload/independent-process and actual-device tests; input remains until its own matching receipt. No offline approval or release authority. |
| P1 / P05 | Remove simulated webhook verification, authoritative stock/freshness claims and header-controlled connector health. Keep unverified source commands disabled. | Disabled/unknown/error UI and API cases; real provider verification only after activation. |
| P1 / P05 | Persist finance, worker inbox/outbox and source reconciliation; separate actual, committed, invoiced, collected and settled states. | Restart-safe accounting-line dedupe and crash-retry proofs, not same-object unit simulations. |
| P01–P05 | Run the nine current update-05 end-to-end journeys and record actual PASS/FAIL/BLOCKED per candidate. | Named actors, exact web/API artifacts, migration ledger, policy versions, connector modes and source evidence. |
| P07 | Human UAT, security review, backup/restore drill, migration rehearsal, monitoring/support ownership and named sign-off. | BLOCKED pending execution and authorized reviewers; local passing tests are not release approval. |

Live Rentals/PurchaseTracker/accounting access remains a separate activation gate: verified API contracts, credentials, mapping, source ownership, idempotency/reconciliation and E3 authorization. No external production records or connectors were activated.

## Verification record

Entries below are chronological candidate records. Their counts and hashes apply to that candidate only; the sixth increment's completed local results are recorded above and in its linked evidence artifact.

- Local PostgreSQL target: loopback development instance, not production.
- Existing schema migration ledger: PostgreSQL 17.11, 16 entries through `0014_enterprise_integration_operations_and_capacity_store.sql`; no migration added in this increment. Full ledger is in the HTTP evidence below.
- First integrated run: 64 test files / 826 tests passed. Further origin/parser follow-up checks are recorded in the final verification entry below.
- Whole-monorepo TypeScript passed before the final origin/parser follow-up.
- Remaining product/production gaps above are OPEN or BLOCKED, not passed by these unit/regression counts.

### Integrated candidate verification

- Whole-monorepo TypeScript: PASS.
- Full regression run: **65 files / 850 tests passed**, 26 September 2026 at 11:48 Asia/Qatar. Local logs: `.local/md-typecheck-final.log` and `.local/md-foundation-tests-final.log`.
- Whole-monorepo build: PASS. Existing web bundle warning remains: approximately 2.53 MB minified / 568 kB gzip; code splitting remains a performance follow-up.
- Rebuilt API restarted on port 4000 with `EOS_ENABLE_LOCAL_SYNTHETIC_AUTH=false`, `NODE_ENV=development`, `ENVIRONMENT=local`.
- **12/12 real HTTP checks passed** using the PostgreSQL-backed session path. [HTTP evidence and migration ledger](../artifacts/md-foundation-http-2026-09-26.md) includes statuses, limits and the preliminary verification helper's accidental synthetic reseed. The corrected final smoke did not reseed or issue business commands.
- The local fixtures were explicitly reseeded after the regression suite. Existing startup/import seeding was also observed and is recorded above as OPEN, not accepted deployment behavior.
- Browser review covered the live approval queue and field queue, including a 390 × 844 viewport, dark/light themes and the updated Arabic queue disclosures. No real approvals, evidence acceptances or external provider commands were issued. [Browser evidence](../artifacts/md-foundation-browser-2026-09-26.md).
- Final browser follow-ups: removed hardcoded accepted-media rows/sample captures, corrected field header/selector/disclosure contrast, localized the queue, and added approval loading/dialog cleanup. Focused web/offline tests: **81 passed**; approval boundaries: **14 passed**. Final web TypeScript, web build and `git diff --check`: PASS. The full 850-test run precedes these small frontend follow-ups; the API/parser candidate was unchanged afterward.

API artifact SHA-256 values (uncommitted local candidate, not a release):

| File | SHA-256 |
|---|---|
| `apps/api/dist/main.js` | `B8AD009D80980972215A779A7471115DE9ABD1B03EB1764E140B02CB84869D33` |
| `apps/api/dist/common/tenant.guard.js` | `13D17ABDFC8B56CBE5CE3D5351BDB21CA5972849D07A2533EEFF638B9FF1D4ED` |
| `apps/api/dist/auth/request-origin.js` | `4305F113D6AF247DEFFBD23588A5360EE1C095A3185B9990A388AA76CAB5F4A3` |
| `apps/web/dist/assets/index-DHz4oA-K.js` | `BD3A07AB941547DDBF2891AB26B8BB4AC9883618079C7C7E2A78ECFEFD9E97D3` |
| `apps/web/dist/assets/index-0yBD8Cc1.css` | `5679C5BC96438397FF9F89EED5FA054769D9811225257DE8198AA8A69B48F152` |

Approval policies and connector contracts have not been activated or certified. The source still uses legacy in-memory policy/approval records; no immutable policy candidate or production sign-off is claimed by this evidence.

### Second candidate verification — 26 September 2026

- Whole-monorepo TypeScript and build: PASS. Regression suite: **70 files / 923 tests passed** at 12:15 Asia/Qatar. Added OpenAPI validation afterward: **5/5 passed**. `git diff --check`: PASS.
- Explicit `pnpm seed` restored the local synthetic fixtures after regression tests and completed with exit 0. No test was run against production.
- [Startup database evidence](../artifacts/seed-startup-database-evidence-2026-09-26.md): **189 tables / 629 rows unchanged** across rebuilt API restart, including full-row fingerprints for users, accounts and memberships; zero seed log markers. Volatile tables are explicitly excluded in that report.
- [Membership HTTP evidence](../artifacts/membership-admin-http-2026-09-26.md): **23 live HTTP/integration checks passed**. Temporary isolated synthetic actors exercised actual middleware/guards, durable revocation, denied next request, receipt replay and post-restart persistence (API PID 29940 replaced by 24120). The replay retained exactly one audit event, outbox intent and receipt. Fixtures and temporary session-token state were removed after verification; no existing member was revoked.
- [Administration browser evidence](../artifacts/membership-admin-browser-2026-09-26.md): scoped directory, loading/error/retry, search, revoked state, disabled incomplete actions, reason-required confirmation, mobile/RTL and theme checks. The confirmation was cancelled; no access was changed through the browser.
- Running API remains `NODE_ENV=development`, `ENVIRONMENT=local`, synthetic authentication off. Preview: `http://localhost:3002/admin/users`. Schema ledger remains at 16 existing entries; no migration was required. The large web bundle warning remains open.

| Final candidate artifact | SHA-256 |
|---|---|
| API identity controller | `EFC623DFC6CB1DF11CA43FBF1781780B5476CE03ADB852E0FFDBBBB97B557CD5` |
| API admin controller | `B0BE5C1D0BC7DB68AE103CC216AF55F418556E7F5305E97E6A64CCB189B77757` |
| DB seed library | `10C3E7046B68B1D0539885BAEC491DBCD289B49CC4778737C09E11543BEB4BA6` |
| Web `index-KRIH49BX.js` | `C27152A8FDEB954FEE7A4F58E0FCDD240F66307CFB14FAB1D93DF8533E384F91` |
| OpenAPI 1.1.0 | `EB8E126EAC0D1C98A504971587B14EA5A39D36518A7BEEC1C0D7DB348E908F29` |

Database-wide append-only audit permissions, external tamper manifests, all-controller project/audience scope, recent MFA, controlled recovery, invitation lifecycle and full policy/approval durability remain OPEN. An inserted outbox event is not proof that a worker delivered a notification. This is another local implementation increment, not P00 closure or production acceptance.

### Third candidate verification — 26 September 2026

- Full regression suite: **73 files / 965 tests passed**, 12:51 Asia/Qatar. Whole-monorepo TypeScript and build: PASS. Final web TypeScript and build also passed after the visual refinements. Logs: `.local/invitation-tests-final.log`, `.local/invitation-typecheck.log`, `.local/invitation-build.log`, and `.local/invitation-web-final-{typecheck,build}.log`.
- Focused backend/contract set: **109 tests passed** across invitation lifecycle, auth lifecycle, audit, membership revocation, admin boundaries and contracts. Client checks include server-only authentication and truthful invitation receipts. New invitation/auth fixtures use isolated synthetic organizations/users and clean exact owned IDs; no external email sends occurred.
- Migration `0015_controlled_invitation_lifecycle.sql` applied to verified loopback PostgreSQL 17.11 in an explicit transaction with its ledger entry. Before/after: **73 users, 73 memberships, 63 accounts**; no identity records created/removed by migration. The provenance constraint was subsequently tightened to reject null audience and aligned with the final migration source. Ledger now has **17 entries**. No seed/reset was run during this increment.
- [Live HTTP invitation evidence](../artifacts/invitation-http-2026-09-26.md): **34/34 checks passed**, covering real middleware, origin, scoped authority, queue redaction, acceptance/cancellation and process restart **PID 4780 → 24088**. Original creation/acceptance receipts and fixture row counts were unchanged after restart; one membership and one acceptance audit remained. Cleanup removed both isolated organizations, all seven owned users and encrypted temporary state, with zero owned rows remaining. This is separate from same-process unit coverage.
- [Browser evidence](../artifacts/invitation-browser-2026-09-26.md) records desktop/mobile English/Arabic, theme, form, keyboard, token-handling and new/existing-account previews. No browser invitation acceptance or password entry was performed.
- Local API: `NODE_ENV=development`, `ENVIRONMENT=local`, `EOS_ENABLE_LOCAL_SYNTHETIC_AUTH=false`; frontend origin localhost:3002. Queue key is protected by the current Windows account's DPAPI in ignored local storage. `scripts/start-local-api.ps1` reloads that local configuration without printing secrets or seeding.
- No actual email delivery, project access grant, privileged invitation approval, production deployment or human UAT sign-off is asserted. Generic migration-runner partial-application handling, all-controller project/audience enforcement, recent MFA and sender/key operations remain OPEN. The web bundle warning remains open.

| Third candidate artifact | SHA-256 |
|---|---|
| API invitation service | `215299B33D759EB21044543DA29A25B5BEACCD262D1A40A56743805B9A5C6C6B` |
| Web `index-Csum4TkH.js` | `8F58A18CF8CBC798ADADF7B0F868B7B648940FBEDE3FDA578A199F0BF38FD9B3` |
| Web stylesheet | `FAE291F990B14D125CA06998F961FEA3A2EBED9D246DBA0F7F3CC595676CB65A` |
| Migration 0015 | `78BD811F42ABF896447A3DE08D61A03C3F0EE6DB02A1F87F77B176EEDEBDB552` |
| OpenAPI 1.2.0 | `A6C105FFAA69E2F7B27AEF01F7C5F5904C111506748A875E349FA559C4AF9F43` |


### Fourth candidate — access administration batch, 26 September 2026

Following the request to build multiple modules before audit, implemented the project-access register, ordinary membership role changes, restoration and role reference together, with connected saved-draft/no-access UI. Then performed one integrated verification phase and focused corrections. Detailed evidence: [access batch verification](../artifacts/access-batch-verification-2026-09-26.md), [live HTTP results](../artifacts/access-batch-http-2026-09-26.json), [migration evidence](../artifacts/access-batch-migrations-2026-09-26.json).

- Durable commands use verified current authority, same-org records, reason, stable command keys and atomic state/audit/outbox/receipt writes. Updates use current record versions.
- Both role change and restoration end target sessions across all organizations/devices. Restoration revokes old project grants; role change retains scope subject to current-role action checks. Self, privileged-role and audience changes are unavailable through these ordinary controls.
- Explicit project grants have no owner/admin bypass or migration backfill. Project directory/detail/cockpit entrypoints are now gated; other business controllers are not certified by this increment. Client internal views are blocked; client portal fabricated financial/signing controls removed.
- Durable project creation saves a draft without automatic access. Unsafe legacy project clone, close and activity update remain explicitly unavailable until rebuilt as durable commands.
- Migration0016+0017 ledger total19, no seed/reset. Whole-monorepo TypeScript/build passed; focused checks resolved failures found by the combined run;36 live HTTP checks passed and exact temporary fixtures cleaned. Browser checked EN/AR, dark/light, mobile and desktop; no existing-user access changes submitted.
- OpenAPI mirrors updated to1.3.0. Local preview remains port3002; API port4000. P00 is not closed, and no release/deployment or delivery-worker success is claimed.

### Fifth candidate — project module batch, 26 September 2026

Implemented and integrated work/task flows, document metadata and revisions, internal report snapshots/history, and scoped portfolio totals. Then ran one combined verification phase with focused corrections. [Detailed evidence](../artifacts/module-batch-verification-2026-09-26.md), [80 live HTTP checks](../artifacts/module-batch-http-2026-09-26.json), and [migration evidence](../artifacts/module-batch-migrations-2026-09-26.json).

- Migrations0018–0020 applied transactionally without grant backfill or identity/project count changes; ledger22. Whole workspace build passed and final TypeScript issues resolved.
- Initial full regression:995passed,6failed plus web transformfailure. All discovered failures were resolved in focused runs:93web/work,17document/contracts,25persistence/constraints; client/route/membershipcontract cases also passed. No second full audit run is claimed.
- All80livechecks passed, including API restart39780→19180, durable receipts, exact scoped reads, stale versions and revocation. Exact temporary fixtures cleaned; no existing-user grants changed.
- Browser checked desktopdark portfolio/navigation and mobileArabiclight reports/accessstates. Current account has zero project grants; populated commands were exercised via isolated HTTPfixtures, not browser changes to existing access. Returned to Englishdark /portfolio.
- Metadata registration does not upload, scan, approve or publish files. Reports are internal draft snapshots; selected periods label current-state captures. Work completion does not accept outputs. Unavailable workflows remain explicit; P00/fullhumanUAT/deployment are not closed.
